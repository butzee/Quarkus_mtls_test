apiVersion: v1
kind: Service
metadata:
  name: {{ include "microservice-d.fullname" . }}
  labels:
    {{- include "microservice-d.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "microservice-d.selectorLabels" . | nindent 4 }}
