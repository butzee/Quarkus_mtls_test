apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microservice-d.fullname" . }}-test-connection"
  labels:
    {{- include "microservice-d.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microservice-d.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
